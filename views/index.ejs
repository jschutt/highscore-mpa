<section class="home-section mb-4 px-4 pb-3">
  <h1 class="text-center mb-4 mt-5 pt-3">Top scores</h1>
  <div class="highscore-list">

    <% users.forEach(user => { %>
      <%- include("shared/partials/user-card.ejs", {user}) %>
    <% }) %>

  </div>
</section>
